What if we gathered up all table data into it's own info graph so that we could search all charts and immediately expose them?

Class:

- A class to handle the main query logic, structure, ai inference from search.

Blocks:

- A block to query the chart post type. It would use <template>, for dynamic rendering? Or should it use server side rendering? Server side rendering may be the only option until we can get CB to be fully iAPI rendered and thus rendered on-demand in the client.
